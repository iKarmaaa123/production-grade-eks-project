apiVersion: v1
kind: Service
metadata:
  name: apps-service
  labels:
    app: apps
spec:
  selector:
    app: apps
  type: ClusterIP
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80