name: CDK Deploy Workflow
on:
  workflow_dispatch:

env:
  AWS_REGION: us-east-1

permissions:
  id-token: write
  contents: read

jobs:
  cdk_deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checking out repository
      uses: actions/checkout@v6

    - name: Setting up node
      uses: actions/setup-node@v6
      with:
        node-version: "25"

    - name: Installing dependencies for cdk project
      run: npm install

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: arn:aws:iam::648767092427:role/IamRoleForGitHubActionsRunner
        aws-region: ${{ env.AWS_REGION }}

    - name: Installing CDK
      run: npm install -g aws-cdk

    - name: Running CDK Deploy
      run: npx cdk deploy NetworkingStack ClusterStack --require-approval never

    - name: Setting up kubectl for GitHub Actions runner
      uses: alexellis/arkade-get@master
      with:
        kubectl: latest
    
    - name: Accessing EKS cluster
      run: aws eks update-kubeconfig --name eks-project-cluster --region us-east-1 --role-arn arn:aws:iam::648767092427:role/ClusterStack-ClusterMasterRole23CC5A5F-8x6RwqBgrWeX
    - name: Deploying ClusterIssuer
      run: kubectl apply -f k8s-manifest-files/cert-manager/issuer.yaml

    - name: Deploying application for argocd
      run: kubectl apply -f k8s-manifest-files/argocd/argocd.yaml

    - name: Deploying Helm Charts
      run: npx cdk deploy HelmStack --require-approval never